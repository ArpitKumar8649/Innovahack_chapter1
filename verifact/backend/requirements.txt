fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
# Phase 6 — semantic layer (local embeddings + vector store)
fastembed>=0.4
chromadb>=0.5
# Phase 7 — knowledge graph (provenance + circular-citation detection)
networkx>=3.2
# Phase 8 — observability & compliance
prometheus-client>=0.20
sentry-sdk>=2.0
